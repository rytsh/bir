---
import Layout from "../../layouts/Layout.astro";
import Base64Tool from "../../components/tools/Base64Tool.svelte";
import { getToolByPath } from "../../data/tools";

const tool = getToolByPath("/codecs/base64");
---

<Layout title="Base Encoding Tool - Base64/Base62/Base58/Base32/Base85" description={tool?.description} keywords={tool?.keywords} currentPath="/codecs/base64">
  <h1 slot="navbar-title">Base Encoding Tool</h1>
  <Base64Tool client:load />
</Layout>
